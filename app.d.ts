// Add here global types for the app
// Its usual for the global type names to have a I or T at the start of the name
// Create global types only its something that is goint to be commonly used, not 
// just for one use or file
type IImageItem = { id: string, url: string };